import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
  display: "swap",
});

export const metadata: Metadata = {
  title: "mello agency — creative engineering studio",
  description:
    "mello agency combines creativity with engineering discipline to build digital products people love. Web, mobile, and AI-powered software.",
  metadataBase: new URL("https://melloagnc.com"),
  openGraph: {
    title: "mello agency — creative engineering studio",
    description:
      "We combine creativity with engineering discipline to build products that users love.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable}`}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}
